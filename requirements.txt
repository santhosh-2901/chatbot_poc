# Pinned to the majors actually verified here. LangChain 1.x is a breaking
# change from the 0.3 API most tutorials still show: the agent entry point is
# langchain.agents.create_agent, not initialize_agent.

# Phase 1 - synthetic data, schemas, tests
pydantic>=2.7
pytest>=8.0

# Phase 2 - financial analysis engine (stdlib only, nothing to add)

# Phases 4-5 - LLM and agent
langchain>=1.2
langchain-core>=1.6
langchain-google-genai>=4.3
langgraph>=1.0
google-genai>=2.10
python-dotenv>=1.0

# Phase 3 - RAG.
# Note: langchain-text-splitters is deliberately NOT used. Importing it pulls in
# sentence-transformers, torch, datasets and pyarrow, which we do not need and
# which segfaulted the interpreter on Windows alongside faiss. app/rag/splitter.py
# replaces it in ~70 lines.
faiss-cpu>=1.8
langchain-community>=0.4

# Phases 6-7 - API and UI
fastapi>=0.115
uvicorn[standard]>=0.30
streamlit>=1.38
altair>=5.0
pandas>=2.0
requests>=2.31
httpx>=0.27

# Optional providers for the fallback chain. Neither is required; entries whose
# provider is unavailable are skipped rather than failing the chain.
#   Groq   - a second hosted provider with a larger free tier
#   Ollama - a local model: no quota, no key, works with no internet at all,
#            but ~160s per exchange on CPU versus ~3s hosted, so it belongs at
#            the end of the chain as insurance rather than as a primary.
# langchain-groq>=0.2
langchain-ollama>=1.1
